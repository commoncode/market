from rea.models import *

"""
Market Agents


Models defined here might make their way to their own app namespace.  Such as:

    + `Store` could be part of `market.stores.Store`, or
    + `Customer` be part of 'market.customers.Customer`

"""

class Store(Agent):
    pass

class Customer(Agent):
    pass
